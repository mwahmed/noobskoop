<%= form_for(@r) do |f| %>
  <% if @r.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@r.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @r.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reply %><br />
    <%= f.text_field :body %>
  </div>
  
  <div class="field">
    <%= f.label :author %><br />
    <%= f.text_field :author %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
