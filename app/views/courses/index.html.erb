<h1>Discussion Board</h1>

<table>
  <tr>
    <th>Courses</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @courses.each do |course| %>
  <tr> 
    <td><%= link_to course.title, course %></td>
    <td><%= "Course code is: #{course.courseCode} "  %></td> 
  </tr>
<% end %>
</table>

<br />

<% link_to 'New Course', new_course_path %>
