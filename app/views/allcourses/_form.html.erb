<%= form_for(@allcourse) do |f| %>
  <% if @allcourse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@allcourse.errors.count, "error") %> prohibited this allcourse from being saved:</h2>

      <ul>
      <% @allcourse.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
